# Drop your video here

Place your final edited video at:

    videos/intro.mp4

It will automatically play on the home page when the user clicks the poster.
If your file has a different name or format, update the `<source>` tag inside `index.html`.

Recommended: H.264 MP4, 1080p, under ~50 MB so GitHub Pages serves it quickly.